[project]
name = "storage"
version = "0.1.0"
description = "Pluggable file storage interface"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
s3 = ["boto3"]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["storage*"]
