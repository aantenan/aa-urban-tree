DATABASE_URL=sqlite:///./local.db
AUTH_PROVIDER=mock

# Fake login accounts (only when AUTH_PROVIDER=mock). One per line or comma-separated.
# Format: email:password  or  email:password:Display Name
# Example: dev@example.com:dev, admin@example.com:admin:Admin User
# If unset, defaults: dev@example.com / dev,  admin@example.com / admin
# MOCK_AUTH_USERS=dev@example.com:dev, admin@example.com:admin

JWT_SECRET=your-secret-key
STORAGE_BACKEND=local
SMTP_HOST=localhost
SMTP_PORT=1025
