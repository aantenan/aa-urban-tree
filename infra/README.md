# Infrastructure

Terraform templates for AWS (VPC, RDS, ECS, ALB, S3, IAM, CloudWatch).

1. Copy `terraform.tfvars.example` to `terraform.tfvars` and set variables.
2. Run `terraform init`, `terraform plan`, `terraform apply` (with remote backend configured as needed).
